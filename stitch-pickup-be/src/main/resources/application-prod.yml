spring:
  jpa:
    show-sql: false
  flyway:
    enabled: true

logging:
  level:
    com.stitchpickup: INFO
    org.springframework.security: WARN

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}
